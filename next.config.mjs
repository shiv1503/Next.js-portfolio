/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add remote domains here if you host screenshots externally
    // e.g. remotePatterns: [{ hostname: 'raw.githubusercontent.com' }]
  },
};

export default nextConfig;
