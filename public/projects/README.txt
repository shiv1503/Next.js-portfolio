Drop project screenshots here as:

  entertainment-buddy.png
  personal-dashboard.png
  safe-drive-application.png
  motobuddy.png

Then set the matching `image: "/projects/<slug>.png"` field on that
project in lib/projects.ts — ProjectCard will automatically swap the
placeholder for a real next/image render.

Also add your resume as /public/resume.pdf so the "Download Resume"
button in the hero works.
